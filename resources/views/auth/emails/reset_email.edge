<p>Click the link bellow to reset your password</p>

@if(user.u_role == 3 && account == false)
   <a href="{{subdomainUrl('accounts', 'password/reset/'+user.u_confirmation_token)}}">Reset Password</a>
@elseif(to.u_role == 4 && account != false)
   <a href="{{subdomainUrl(account.a_slug, 'password/reset/'+user.u_confirmation_token)}}">Reset Password</a>
@else
   <a href="{{subdomainUrl('admin', 'password/reset/'+user.u_confirmation_token)}}">Reset Password</a>
@endif